# Rust
/target/
**/*.rs.bk
*.pdb

# Node / frontend
node_modules/
ui/dist/
ui/.vite/
*.tsbuildinfo

# OpenBiz runtime data (embedded Oxigraph store, backups)
/data/
*.oxigraph/

# A local configuration file is a developer's own deployment, not the project's. The documented
# example lives in docs/CONFIGURATION.md.
/openbiz.toml

# Environment & secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# Loop scratch
/.loop-scratch/
